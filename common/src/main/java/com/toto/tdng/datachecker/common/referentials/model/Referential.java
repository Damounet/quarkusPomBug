package com.toto.tdng.datachecker.common.referentials.model;

// Reference files on Minio use those names (with ".XML" at the end), make sur to update files name of all environment if you refactor the enum names.
public enum Referential {
    DMRL,
    CIR_SUPPLY,
    CIR_PART,
    CIR_TOOL
}
